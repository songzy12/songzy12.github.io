<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">

  <title><![CDATA[Category: C | Zhengyang Song's Notepad]]></title>
  <link href="http://songzy12.github.io/blog/categories/c/atom.xml" rel="self"/>
  <link href="http://songzy12.github.io/"/>
  <updated>2018-10-06T14:27:13+08:00</updated>
  <id>http://songzy12.github.io/</id>
  <author>
    <name><![CDATA[Zhengyang Song]]></name>
    
  </author>
  <generator uri="http://octopress.org/">Octopress</generator>

  
  <entry>
    <title type="html"><![CDATA[C]]></title>
    <link href="http://songzy12.github.io/blog/2016/09/17/c/"/>
    <updated>2016-09-17T12:55:45+08:00</updated>
    <id>http://songzy12.github.io/blog/2016/09/17/c</id>
    <content type="html"><![CDATA[<h2>climits</h2>

<pre><code>INT_MAX
</code></pre>

<h2>cmath</h2>

<h3>hypot</h3>

<pre><code>#include &lt;cmath&gt;
// distance of (x, y) and (X, Y)
return hypot(x - X, y - Y); 
</code></pre>

<h2>cstdlib</h2>

<h3>atoi</h3>

<p>Ascii to Integer.</p>

<pre><code>int atoi (const char * str);
</code></pre>

<pre><code>#include &lt;cstdio&gt;
#include &lt;cstdlib&gt;

char buffer[256];
fgets(buffer, 256, stdin);
int i = atoi(buffer);
</code></pre>

<h3>bsearch</h3>

<pre><code>void* bsearch (const void* key, const void* base,
               size_t num, size_t size,
               int (*compar)(const void*,const void*));

Searches the given key in the array pointed to by base (which is formed by num elements, each of size bytes), and returns a void* pointer to a matching element, if found.
</code></pre>

<h3>fgets</h3>

<pre><code>char * fgets ( char * str, int num, FILE * stream );
</code></pre>

<p>Reads characters from <code>stream</code> and stores them as a C string into <code>str</code> until (<code>num-1</code>) characters have been read or either a <code>newline</code> or the <code>end-of-file</code> is reached, whichever happens first.</p>

<h3>gets</h3>

<pre><code>char * gets ( char * str );
</code></pre>

<p>Notice that <code>gets</code> is quite different from <code>fgets</code>: not only gets uses <code>stdin</code> as source, but it does not include the ending newline character in the resulting string and does not allow to specify a maximum size for <code>str</code> (which can lead to buffer overflows).</p>

<h3>printf</h3>

<ul>
<li><code>%g</code>: Use the shortest representation: <code>%e</code> or <code>%f</code></li>
<li><code>-</code>: Left-justify within the given field width; Right justification is the default (see width sub-specifier).</li>
</ul>


<h3>qsort</h3>

<pre><code>void qsort (void* base, size_t num, size_t size,
            int (*compar)(const void*,const void*));

Sorts the num elements of the array pointed to by base, each element size bytes long, using the compar function to determine the order.
</code></pre>

<h3>strtod</h3>

<p>string to double.</p>

<pre><code>char szOrbits[] = "365.24 29.53";
char* pEnd;
double d1, d2;
d1 = strtod (szOrbits, &amp;pEnd);
d2 = strtod (pEnd, NULL);
</code></pre>

<p>Parses the C-string <code>str</code> interpreting its content as a floating point number (according to the current locale) and returns its value as a <code>double</code>. If <code>endptr</code> is not a null pointer, the function also sets the value of <code>endptr</code> to point to the first character after the number.</p>

<h2>cstring</h2>

<h3>strtok</h3>

<p>string to tokens.</p>

<pre><code>#include &lt;cstring&gt;
using namespace std;

const char *delim = ",";
for (int i = 0; i &lt; edge_num; ++i) {
    if (i != edge_num - 1) 
        topo[i][strlen(topo[i])-1] = 0;
    char *token = strtok(topo[i], delim);
    while (token) {
        cout&lt;&lt;token&lt;&lt;" ";
        token = strtok(0, delim);
    }
    cout&lt;&lt;endl;    
}
</code></pre>

<h3>memmove</h3>

<pre><code>void * memmove ( void * destination, const void * source, size_t num );

Copies the values of num bytes from the location pointed by source to the memory block pointed by destination. 
Copying takes place as if an intermediate buffer were used, allowing the destination and source to overlap.
</code></pre>

<h2>ctime</h2>

<pre><code>struct tm time_cha;
time_t t1;
int year, month, day,hour,min,sec;
init();
scanf("%d-%d-%d %d:%d:%d",&amp;year,&amp;month,&amp;day,&amp;hour,&amp;min,&amp;sec);
time_cha.tm_year = year - 1900; 
time_cha.tm_mon  = month - 1;
time_cha.tm_mday = day;
time_cha.tm_hour = hour;
time_cha.tm_min  = min;
time_cha.tm_sec  = sec;
t1 = mktime(&amp;time_cha);
</code></pre>
]]></content>
  </entry>
  
  <entry>
    <title type="html"><![CDATA[GCC]]></title>
    <link href="http://songzy12.github.io/blog/2016/08/13/gcc/"/>
    <updated>2016-08-13T14:17:43+08:00</updated>
    <id>http://songzy12.github.io/blog/2016/08/13/gcc</id>
    <content type="html"><![CDATA[<p><a href="http://wiki.ubuntu.org.cn/index.php?title=Compiling_C&amp;variant=zh-my">here</a></p>

<p><code>.a</code>    静态库 (archive)
<code>.c</code>    C源代码（需要编译预处理）
<code>.h</code>    C源代码头文件
<code>.i</code>    C源代码（不需编译预处理）
<code>.o</code>    对象文件
<code>.s</code>    汇编语言代码
<code>.so</code>   动态库(shared object)</p>

<p>一步到位的编译指令是:</p>

<pre><code>gcc test.c -o test
</code></pre>

<h2>Preprocessing</h2>

<pre><code>gcc -E test.c -o test.i
</code></pre>

<p>gcc的-E选项，可以让编译器在预处理后停止，并输出预处理结果。</p>

<h2>Compilation</h2>

<pre><code>gcc -S test.i -o test.s
</code></pre>

<p>gcc的-S选项，表示在程序编译期间，在生成汇编代码后，停止，-o输出汇编代码文件。</p>

<p>汇编语言的形式依赖于编译器的目标平台。如果多个源码文件被编译，每个文件将分别产生对应的汇编代码模块。</p>

<h2>Assembly</h2>

<pre><code>gcc -c test.s -o test.o
</code></pre>

<p>选项 <code>-c</code> 指示 GCC 编译源代码文件，但将对象文件保留在磁盘中并跳过链接对象文件生成可执行文件这一步。在这种情况下，默认的输出文件的文件名同源代码文件名一致，只不过後缀换为 <code>.o</code> 。</p>

<p>当构建对象库或者生成一系列对象文件以备稍後链接用时，一条命令即可从多个源码文件生成对应的对象文件。</p>

<h2>Linking</h2>

<pre><code>gcc test.o -o test
</code></pre>

<p><code>gcc</code>连接器是<code>gas</code>提供的，负责将程序的目标文件与所需的所有附加的目标文件连接起来，最终生成可执行文件。附加的目标文件包括静态连接库和动态连接库。</p>

<p>静态库链接时搜索路径顺序：</p>

<ol>
<li>ld会去找GCC命令中的参数<code>-L</code></li>
<li>再找gcc的环境变量<code>LIBRARY_PATH</code></li>
<li>再找内定目录 <code>/lib /usr/lib /usr/local/lib</code> 这是当初compile gcc时写在程序内的</li>
</ol>


<p>动态链接时、执行时搜索路径顺序:</p>

<ol>
<li>编译目标代码时指定的动态库搜索路径</li>
<li>环境变量<code>LD_LIBRARY_PATH</code>指定的动态库搜索路径</li>
<li>配置文件<code>/etc/ld.so.conf</code>中指定的动态库搜索路径</li>
<li>默认的动态库搜索路径<code>/lib</code></li>
<li>默认的动态库搜索路径<code>/usr/lib</code></li>
</ol>


<p>有关环境变量：
<code>LIBRARY_PATH</code>环境变量：指定程序静态链接库文件搜索路径
<code>LD_LIBRARY_PATH</code>环境变量：指定程序动态链接库文件搜索路径</p>

<p>When you link against the <code>*.a</code>, the code from the library is included in the executable itself and the executable can be run without requiring the <code>*.a</code> file to be present. When you link against the <code>*.so</code>, that is not the case and the <code>*.so</code> file must be present at runtime.</p>

<h2>静态库</h2>

<p>静态库是编译器生成的普通的 <code>.o</code> 文件的集合。链接一个程序时用库中的对象文件还是目录中的对象文件都是一样的。静态库的另一个名字叫归档文件(<code>archive</code>)，管理这种归档文件的工具叫 <code>ar</code> 。</p>

<p>这两个源码文件可以用以下命令编译成对象文件：</p>

<pre><code>gcc -c -Wall hellofirst.c hellosecond.c
</code></pre>

<p>程序 <code>ar</code> 配合参数 <code>-r</code> 可以创建一个新库并将对象文件插入。如果库不存在的话，参数 <code>-r</code> 将创建一个新的，并将对象模块添加（如有必要，通过替换）到归档文件中。下面的命令将创建一个包含本例中两个对象模块的名为 <code>libhello.a</code> 的静态库：</p>

<pre><code>ar -r libhello.a hellofirst.o hellosecond.o
</code></pre>

<p>程序 twohellos 可以通过在命令行中指定库用一条命令来编译和链接，命令如下：</p>

<pre><code>gcc twohellos.c libhello.a -o twohellos
</code></pre>

<p>静态库的命名惯例是名字以三个字母 <code>lib</code> 开头并以後缀 <code>.a</code> 结束。所有的系统库都采用这种命名惯例，并且它允许通过 <code>-l(ell)</code> 选项来简写命令行中的库名。下面的命令与先前命令的区别仅在于 <code>gcc</code> 期望的找寻该库的位置不同：</p>

<pre><code>gcc twohellos.c -lhello -o twohellos
</code></pre>

<p>选项 <code>-l</code> 不具有指定路径的能力，但是它要求编译器在系统库目录下找寻该库。 选项<code>-L</code>可以指定具体的目录，使编译器在搜寻系统库目录前先搜寻此目录</p>

<h2>共享库</h2>

<p>对象文件模块中所有地址（变量引用或函数调用）都是相对而不是绝对的，这使得共享模块可以在程序的运行过程中被动态地调用和执行。</p>

<p>要将以上两个源码文件编译成对象文件，可以用下面的命令：</p>

<pre><code>gcc -c -fpic shellofirst.c shellosecond.c
</code></pre>

<p>选项 <code>-c</code> 告诉编译器只生成 <code>.o</code> 的对象文件。 选项 <code>-fpic</code> 使生成的对象模块采用浮动的（可重定位的）地址。缩微词 <code>pic</code> 代表“位置无关代码”（position independent code）。</p>

<p>下面的 gcc 命令将对象文件构建成一个名为 <code>hello.so</code> 的共享库：</p>

<pre><code>gcc -shared shellofirst.o shellosecond.o -o hello.so
</code></pre>

<p>通常情况下，链接器定位并使用 main() 函数作为程序的入口，但是本例中输出模块中没有这种入口点，为抑制错误选项 <code>-shared</code> 是必须的。</p>

<p>下面的命令直接将模块编译并存储为共享库：</p>

<pre><code>gcc -fpic -shared shellofirst.c shellosecond.c -o hello.so
</code></pre>

<p>该程序可以用下面的命令编译并链接共享库：</p>

<pre><code>gcc stwohellos.c hello.so -o stwohellos
</code></pre>

<p>程序 <code>stwohello</code> 已经完成，但要运行它必须让其能定位到共享库 <code>hello.so</code>，因为库中的函数要在程序运行时被加载。 需要注意的是，当前工作目录可能不在共享库的查找路径中，因此需要使用如下的命令行设定环境变量<code>LD_LIBRARY_PATH</code>：</p>

<pre><code>export LD_LIBRARY_PATH=$LD_LIBRARY_PATH:./
</code></pre>
]]></content>
  </entry>
  
</feed>
