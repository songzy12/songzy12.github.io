<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">

  <title><![CDATA[Category: Python | Zhengyang Song's Notepad]]></title>
  <link href="http://songzy12.github.io/blog/categories/python/atom.xml" rel="self"/>
  <link href="http://songzy12.github.io/"/>
  <updated>2018-10-06T13:59:48+08:00</updated>
  <id>http://songzy12.github.io/</id>
  <author>
    <name><![CDATA[Zhengyang Song]]></name>
    
  </author>
  <generator uri="http://octopress.org/">Octopress</generator>

  
  <entry>
    <title type="html"><![CDATA[Generic Operating System Services]]></title>
    <link href="http://songzy12.github.io/blog/2018/04/16/generic-operating-system-services/"/>
    <updated>2018-04-16T14:19:01+08:00</updated>
    <id>http://songzy12.github.io/blog/2018/04/16/generic-operating-system-services</id>
    <content type="html"><![CDATA[<h2>io</h2>

<pre><code>import io
io.open(file, mode='r', buffering=-1, encoding=None, errors=None, newline=None, closefd=True)
</code></pre>

<ul>
<li>The <code>open()</code> function an alias to <code>io.open()</code>. It wraps the OS-level file descriptor in a file object.</li>
<li><code>os.open()</code> is just a wrapper for the lower-level POSIX syscall. It returns the file descriptor (a number) that represents the opened file.</li>
</ul>


<h2>logging</h2>

<pre><code>import logging  

logging.basicConfig()
logger = logging.getLogger('test')  
# NOTSET &lt; DEBUG &lt; INFO &lt; WARNING &lt; ERROR &lt; CRITICAL
logger.setLevel(logging.DEBUG)  

fh = logging.FileHandler('test.log')  
fh.setLevel(logging.DEBUG)  

ch = logging.StreamHandler()  
ch.setLevel(logging.DEBUG)  

formatter = logging.Formatter('%(asctime)s - %(name)s - %(levelname)s - %(message)s')  
fh.setFormatter(formatter)  
ch.setFormatter(formatter)  

logger.addHandler(fh)  
logger.addHandler(ch)  

# logger.debug, logger.info, logger.warning, logger.error, logger.critical
logger.info('foorbar')  
</code></pre>

<h2>optparse</h2>

<pre><code>parser = optparse.OptionParser(sys.argv[0]+' '+
'-r &lt;file_with URLs&gt;')
parser.add_option('-r', dest='URLs', type='string', 
        help='specify target file with URLs')
(options, args) = parser.parse_args()
URLs=options.URLs
</code></pre>

<h2>os</h2>

<pre><code>import os
script_dir = os.path.dirname(__file__) # &lt;-- absolute dir the script is in
rel_path = "2091/data.txt"
abs_file_path = os.path.join(script_dir, rel_path)
</code></pre>

<h3>makedirs</h3>

<p>Works like mkdir, except that any intermediate path segment (not just the rightmost) will be created if it does not exist.</p>

<pre><code>if not os.path.exists(directory):
    os.makedirs(directory)
</code></pre>

<h3>path</h3>

<pre><code>&gt;&gt;&gt; filename, file_extension = os.path.splitext('/path/to/somefile.ext')
&gt;&gt;&gt; filename
'/path/to/somefile'
&gt;&gt;&gt; file_extension
'.ext'
</code></pre>

<pre><code>os.path.join(dir_name, file_name);
</code></pre>

<pre><code>import sys
import os
parent = os.path.dirname(os.path.dirname(os.path.abspath(__file__)))
sys.path.insert(0, parent)
</code></pre>

<pre><code>os.path.dirname(path)
    Return the directory name of pathname path. This is the first element of the pair returned by passing path to the function split().
</code></pre>

<pre><code>os.access(path, mode);
</code></pre>

<ul>
<li>os.F_OK: test the existence of path</li>
<li>os.R_OK: readability</li>
<li>os.W_OK: writability</li>
<li>os.X_OK: executability</li>
</ul>


<h3>popen</h3>

<pre><code>output = os.popen('cat /proc/cpuinfo')
print output.read()
</code></pre>

<h3>system</h3>

<pre><code>retvalue = os.system("ps -p 2993 -o time --no-headers")
</code></pre>

<h2>pickle</h2>

<pre><code>import pickle

with open("active_user.pkl", "wb") as f:
    pickle.dump(active_user, f)

with open("active_user") as f:
    active_user = pickle.load(f)

with open("text1") as f1, open("text2") as f2:
    pass
</code></pre>
]]></content>
  </entry>
  
  <entry>
    <title type="html"><![CDATA[Sklearn]]></title>
    <link href="http://songzy12.github.io/blog/2017/06/23/sklearn/"/>
    <updated>2017-06-23T17:17:44+08:00</updated>
    <id>http://songzy12.github.io/blog/2017/06/23/sklearn</id>
    <content type="html"><![CDATA[<h2>feature_extraction</h2>

<pre><code>from sklearn.feature_extraction.text import CountVectorizer
vectorizer = CountVectorizer()
corpus = [
        'This is the first document.',
        'This is the second second document.',
        'And the third one.',
        'Is this the first document?',
    ]
X = vectorizer.fit_transform(corpus)
X.toarray()
vectorizer.get_feature_names()
vectorizer.vocabulary_
</code></pre>

<pre><code>from sklearn.feature_extraction.text import TfidfTransformer
transformer = TfidfTransformer()
counts = [[3, 0, 1],
           [2, 0, 0],
           [3, 0, 0],
           [4, 0, 0],
           [3, 2, 0],
           [3, 0, 2]]
tfidf = transformer.fit_transform(counts)
tfidf.toarray()
</code></pre>

<h2>linear_model</h2>

<pre><code>&gt;&gt;&gt; import numpy as np
&gt;&gt;&gt; from sklearn import linear_model
&gt;&gt;&gt; X = np.array([[-1, -1], [-2, -1], [1, 1], [2, 1]])
&gt;&gt;&gt; Y = np.array([1, 1, 2, 2])
&gt;&gt;&gt; clf = linear_model.SGDClassifier()
&gt;&gt;&gt; clf.fit(X, Y)
print(clf.predict([[-0.8, -1]]))
</code></pre>

<h2>cluster</h2>

<pre><code>from sklearn.cluster import KMeans
kmeans = KMeans(n_clusters=n_digits, n_init=10)
kmeans.fit(reduced_data)
centroids = kmeans.cluster_centers_
labels = kmeans.labels_
inertia_ = kmeans.inertia_
</code></pre>

<h2>decomposition</h2>

<pre><code>import numpy as np
from sklearn.decomposition import PCA
X = np.array([[-1, -1], [-2, -1], [-3, -2], [1, 1], [2, 1], [3, 2]])
pca = PCA(n_components=2)
pca.fit(X)
print(pca.explained_variance_ratio_) 
</code></pre>

<h2>ensemble</h2>

<pre><code>from sklearn.ensemble import RandomForestClassifier

dataset = genfromtxt(open('data/train.csv','r'), delimiter=',', dtype='f8')[1:]
target = [x[0] for x in dataset]
train = [x[1:] for x in dataset]
test = genfromtxt(open('data/test.csv','r'), delimiter=',', dtype='f8')[1:]

rf = RandomForestClassifier(n_estimators=100)
rf.fit(train, target)

predicted_digits = rf.predict(test)
</code></pre>
]]></content>
  </entry>
  
  <entry>
    <title type="html"><![CDATA[String Services]]></title>
    <link href="http://songzy12.github.io/blog/2016/12/24/string-services/"/>
    <updated>2016-12-24T14:29:03+08:00</updated>
    <id>http://songzy12.github.io/blog/2016/12/24/string-services</id>
    <content type="html"><![CDATA[<h2>re</h2>

<h3>?P</h3>

<pre><code>&gt;&gt;&gt; import re
&gt;&gt;&gt; match = re.search('(?P&lt;name&gt;.*) (?P&lt;phone&gt;.*)', 'John 123456')
&gt;&gt;&gt; match.group('name')
'John'
</code></pre>

<h3>split</h3>

<pre><code>re.split(u'，|。', u'a，b。c')
</code></pre>

<pre><code>import re
DATA = "Hey, you - what are you doing here!?"
print re.findall(r"[\w']+", DATA)
</code></pre>

<h3>sub</h3>

<pre><code>re.sub(pattern, repl, string, count=0, flags=0)

    Return the string obtained by replacing the leftmost non-overlapping occurrences of pattern in string by the replacement repl. If the pattern isn’t found, string is returned unchanged. repl can be a string or a function;
    If repl is a function, it is called for every non-overlapping occurrence of pattern. The function takes a single match object argument, and returns the replacement string.
</code></pre>

<pre><code>&gt;&gt;&gt; def dashrepl(matchobj):
...     if matchobj.group(0) == '-': return ' '
...     else: return '-'
&gt;&gt;&gt; re.sub('-{1,2}', dashrepl, 'pro----gram-files')
'pro--gram files'
</code></pre>
]]></content>
  </entry>
  
  <entry>
    <title type="html"><![CDATA[Data Compression and Archiving]]></title>
    <link href="http://songzy12.github.io/blog/2016/12/24/data-compression-and-archiving/"/>
    <updated>2016-12-24T14:28:16+08:00</updated>
    <id>http://songzy12.github.io/blog/2016/12/24/data-compression-and-archiving</id>
    <content type="html"><![CDATA[<h2>zipfile</h2>

<pre><code>print f.read(f.namelist()[0])
</code></pre>
]]></content>
  </entry>
  
  <entry>
    <title type="html"><![CDATA[Python Runtime Services]]></title>
    <link href="http://songzy12.github.io/blog/2016/12/24/python-runtime-services/"/>
    <updated>2016-12-24T14:26:34+08:00</updated>
    <id>http://songzy12.github.io/blog/2016/12/24/python-runtime-services</id>
    <content type="html"><![CDATA[<h2>sys</h2>

<pre><code>import sys
f_in = open('in.txt', 'r+')
f_out = open('out.txt', 'w+')
sys.stdin = f_in
sys.stdout = f_out

f_in.close()
f_out.close()
</code></pre>

<h2>traceback</h2>

<pre><code>try:
    execfile("test.py")
except Exception as e:
    import traceback, code
    traceback.print_exc()
    with open('log.txt', 'w') as f:
        traceback.print_exc(file=f)
    code.interact(local=dict(globals(), **locals()))
</code></pre>
]]></content>
  </entry>
  
</feed>
