---
layout: post
title: "Django"
date: 2016-11-18 14:19:20 +0800
comments: true
categories: Python

---

[Here](http://jeffknupp.com/blog/2013/12/18/starting-a-django-16-project-the-right-way/).

## virtualenv

```
sudo pip install vietualenvwrapper
source /usr/local/bin/virtualenvwrapper.sh
```

```
mkvirtualenv django_project
deactivate
workon django_project
```

## django

```
pip install django
which django-admin.py
```

```
django-admin.py startproject django_project
```

## south

```
pip install south
```

Tutorial [here](https://south.readthedocs.io/en/latest/tutorial/).

All migrations to our own apps will be done using South, including the "initial" migration.

```
pip freeze > requirements.txt
```

`vi settings.py`

```
INSTALLED_APPS = DEFAULT_APPS + THIRD_PARTY_APPS + LOCAL_APPS
```

## fabric

```
pip install fabric
```

`vi fabfile.py`

```
from fabric.api import local, lcd

def prepare_deployment(branch_name):
    local('python manage.py test django_project')
    local('git add -p && git commit') # or local('hg add && hg commit')
    
def deploy():
    with lcd('/path/to/my/prod/area/'):

        # With git...
        local('git pull /my/path/to/dev/area/')

        # With Mercurial...
        local('hg pull /my/path/to/dev/area/')
        local('hg update')

        # With both
        local('python manage.py migrate myapp')
        local('python manage.py test myapp')
        local('/my/command/to/restart/webserver')
```

```
$ fab prepare_deployment
$ fab deploy
```

## unittest

```
vi test_<appname>.py
```

```
import datetime

from django.test import TestCase
from myapp.models import Post

class BlogPostTestCase(TestCase):
    def setUp(self):
        Post.objects.create(id=1, 
            title='Starting a Django 1.6 Project the Right Way', 
            date=datetime.datetime.now(),
            category='Django')
        Post.objects.create(id=2, 
            title='Python\'s Hardest Problem', 
            date=datetime.datetime.now(),
            category='Python')

    def test_posts_have_category(self):
        """Animals that can speak are correctly identified"""
        first_post = Post.objects.get(id=1)
        second_post = Post.objects.get(id=2)
        self.assertEqual(first_post.category, 'Django')
        self.assertEqual(second_post.category, 'Python')
```

```
./manage.py test <appname>
```
